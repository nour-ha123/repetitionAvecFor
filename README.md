# repetitionAvecFor

## Basic

---

## 1 — Somme de N

Écris une fonction appelée **sommeDeN** qui prend un nombre comme paramètre et retourne la somme de ce nombre et de tous les nombres qui le précèdent.

```javascript
function sommeDeN(n) {
   // Écris ton code ici
}
```

Tests attendus :

```javascript
sommeDeN(5); // => 15
sommeDeN(3); // => 6
```

---

## 2 — Factorielle de N

Écris une fonction appelée **factorielleDeN** qui prend un nombre entier comme paramètre et retourne la factorielle de ce nombre.

```javascript
function factorielleDeN(n) {
   // Écris ton code ici
}
```

Tests attendus :

```javascript
factorielleDeN(5); // => 120
factorielleDeN(3); // => 6
factorielleDeN(1); // => 1
factorielleDeN(0); // => 1
```

---

## 3 — Répéter une chaîne

Écris une fonction appelée **répéterChaîne** qui prend deux paramètres :

* une chaîne de caractères
* un nombre

La fonction retourne la chaîne répétée le nombre de fois spécifié.

```javascript
function répéterChaîne(chaîne, nombre) {
   // Écris ton code ici
}
```

Tests attendus :

```javascript
répéterChaîne("Hi", 5);  // => "HiHiHiHiHi"
répéterChaîne("dog", 3); // => "dogdogdog"
```

---

## 4 — Somme de Min à Max

Écris une fonction appelée **sommeMinÀMax** qui prend deux nombres comme paramètres et retourne la somme des entiers :

* en commençant par le minimum
* en excluant le maximum

```javascript
function sommeMinÀMax(minimum, maximum) {
   // Écris ton code ici
}
```

Tests attendus :

```javascript
sommeMinÀMax(1, 5); // => 10
sommeMinÀMax(3, 7); // => 18
```

---

## 5 — Produit de Min à Max

Écris une fonction appelée **produitMinÀMax** qui prend deux nombres comme paramètres et retourne le produit des entiers :

* en commençant par le minimum
* en excluant le maximum

```javascript
function produitMinÀMax(minimum, maximum) {
   // Écris ton code ici
}
```

Tests attendus :

```javascript
produitMinÀMax(1, 5); // => 24
produitMinÀMax(3, 7); // => 360
```

 ## Plus de pratique


## 1 — Multiplier par 10 N fois

Écrivez une fonction appelée multiplierPar10NFois qui prend deux nombres comme paramètres et renvoie le premier nombre multiplié par 10, répété le nombre de fois indiqué par le deuxième nombre.

```javascript
function multiplierPar10NFois(num, n) {
   // Écrivez votre code ici
}
```

Exemple attendu :

```javascript
multiplierPar10NFois(3, 3); // => 3000
multiplierPar10NFois(5, 2); // => 500
```

---

## 2 — Compter un caractère par index

Écrivez une fonction appelée compterCaractèreÀIndex qui prend trois paramètres : une chaîne de caractères, un index et une autre chaîne de caractères.

La fonction doit récupérer le caractère correspondant à l’index dans la première chaîne, puis compter combien de fois ce caractère apparaît dans la deuxième chaîne.

```javascript
function compterCaractèreÀIndex(string1, index, string2) {
   // Écrivez votre code ici
}
```

Exemple attendu :

```javascript
compterCaractèreÀIndex("hello", 2, "lol");          // => 2
compterCaractèreÀIndex("world", 3, "Hello World"); // => 3
```

---

## 3 — Inverser une chaîne

Écrivez une fonction appelée inverserChaîne qui prend une chaîne de caractères en entrée et retourne cette chaîne inversée.

```javascript
function inverserChaîne(chaine) {
   // Écrivez votre code ici
}
```

Exemples attendus :

```javascript
inverserChaîne("hello"); // => "olleh"
inverserChaîne("world"); // => "dlrow"
```

---

## 4 — Obtenir l’indice d’un caractère

Écrivez une fonction appelée obtenirIndiceDe qui prend deux paramètres :

* une chaîne de caractères
* un caractère

La fonction retourne la **première position** de ce caractère dans la chaîne.

```javascript
function obtenirIndiceDe(chaine, caractere) {
   // Écrivez votre code ici
}
```

Exemples attendus :

```javascript
obtenirIndiceDe("I am a hacker", "a"); // => 2
obtenirIndiceDe("hello", "h");         // => 0
```

---

## 5 — Somme des nombres pairs

Écrivez une fonction appelée sommePairs qui prend deux nombres comme paramètres et retourne la somme de tous les nombres pairs en commençant à nombre1 (inclus) et en excluant nombre2.

```javascript
function sommePairs(nombre1, nombre2) {
   // Écrivez votre code ici
}
```

Exemples attendus :

```javascript
sommePairs(3, 9); // => 18
sommePairs(1, 6); // => 6
```

---


---

